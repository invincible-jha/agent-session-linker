{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-session-linker.serviceAccountName" . }}
  labels:
    {{- include "agent-session-linker.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
